<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acro Network - Minecraft Server</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <link rel="stylesheet" type="text/css" href="./static/css/navbarStyle.css">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#1E293B', // Dark gray
                        secondary: '#7DD3FC', // Light blue
                    }
                }
            }
        }
    </script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
        
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #0F172A;
            color: #E2E8F0;
        }
        
        .gradient-text {
            background: linear-gradient(90deg, #7DD3FC, #38BDF8);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }
        
        .card-hover:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(29, 78, 216, 0.3);
        }
        
        .transition-all {
            transition: all 0.3s ease;
        }
        
        /* Mobile menu animation */
        .mobile-menu {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease-out;
        }
        
        .mobile-menu.open {
            max-height: 500px;
            transition: max-height 0.5s ease-in;
        }
        
        /* Hamburger animation */
        .hamburger span {
            display: block;
            width: 24px;
            height: 2px;
            background-color: white;
            transition: all 0.3s ease;
        }
        
        .hamburger span:nth-child(2) {
            margin: 6px 0;
        }
        
        .hamburger.open span:nth-child(1) {
            transform: rotate(45deg) translate(5px, 5px);
        }
        
        .hamburger.open span:nth-child(2) {
            opacity: 0;
        }
        
        .hamburger.open span:nth-child(3) {
            transform: rotate(-45deg) translate(5px, -5px);
        }

        /* Discord presence indicators */
        .presence-online {
            background-color: #3BA55D;
        }
        
        .presence-idle {
            background-color: #FAA61A;
        }
        
        .presence-dnd {
            background-color: #ED4245;
        }
        
        .presence-offline {
            background-color: #747F8D;
        }
        
        /* Staff card styling */
        .staff-card {
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .staff-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
            border-color: rgba(125, 211, 252, 0.3);
        }
        
        /* Role badges */
        .role-badge {
            font-size: 0.7rem;
            font-weight: 600;
            padding: 0.2rem 0.5rem;
            border-radius: 9999px;
            text-transform: uppercase;
        }
        
        .role-owner {
            background-color: #5865F2;
        }
        
        .role-admin {
            background-color: #ED4245;
        }
        
        .role-moderator {
            background-color: #3BA55D;
        }
        
        .role-contributor {
            background-color: #FAA61A;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <%- include("./layouts/navbar.ejs"); %>

    <!-- Hero Section -->
    <div class="relative bg-gradient-to-b from-gray-900 to-gray-800 py-20">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
                <h2 class="text-3xl font-bold text-white">Our Strong Team</h2>
                <p class="text-gray-400 mt-2 max-w-2xl mx-auto">
                    Meet the dedicated team that keeps Acro Network running smoothly.
                </p>
            </div>
            
            <!-- Owners -->
            <div class="mb-12">
                <div class="flex items-center mb-6">
                    <div class="w-1 h-8 bg-blue-500 mr-4 rounded-full"></div>
                    <h3 class="text-2xl font-bold text-white">Owners</h3>
                </div>
                
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                    <% rootMembers.forEach(member => { %>
                    <div class="staff-card bg-gray-800 rounded-lg p-6 flex items-center">
                        <div class="relative mr-4">
                            <img src="<%= member.avatar %>" 
                                 alt="<%= member.username %> Avatar" 
                                 class="w-16 h-16 rounded-full border-2 border-blue-500">
                            <div class="absolute bottom-0 right-0 w-4 h-4 
                            <%= member.presence === 'online' ? 'bg-green-500' : member.presence === 'idle' ? 'bg-yellow-500' : member.presence === 'dnd' ? 'bg-red-500' : 'bg-gray-500' %> 
                            rounded-full border-2 border-gray-800"></div>
                        </div>
                        <div>
                            <div class="flex items-center">
                                <h4 class="text-lg font-semibold text-white"><%= member.globalName %></h4>
                                <span class="role-badge role-owner ml-2">Owner</span>
                            </div>
                            <p class="text-gray-400 text-sm">@<%= member.username %></p>
                        </div>
                    </div>
                
                  <% }) %>
                </div>
            </div>
            
            <!-- Administrators -->
            <div class="mb-12">
                <div class="flex items-center mb-6">
                    <div class="w-1 h-8 bg-red-500 mr-4 rounded-full"></div>
                    <h3 class="text-2xl font-bold text-white">Administrators</h3>
                </div>
                
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                    <% adminMembers.forEach(member => { %>
                    <div class="staff-card bg-gray-800 rounded-lg p-6 flex items-center">
                        <div class="relative mr-4">
                            <img src="<%= member.avatar %>" 
                                 alt="<%= member.username %> Avatar" 
                                 class="w-16 h-16 rounded-full border-2 border-red-500">
                            <div class="absolute bottom-0 right-0 w-4 h-4 
                            <%= member.presence === 'online' ? 'bg-green-500' : member.presence === 'idle' ? 'bg-yellow-500' : member.presence === 'dnd' ? 'bg-red-500' : 'bg-gray-500' %> 
                            rounded-full border-2 border-gray-800"></div>
                        </div>
                        <div>
                            <div class="flex items-center">
                                <h4 class="text-lg font-semibold text-white"><%= member.globalName %></h4>
                                <span class="role-badge role-admin ml-2">Admin</span>
                            </div>
                            <p class="text-gray-400 text-sm">@<%= member.username %></p>
                        </div>
                    </div>
                    <% }) %>
                </div>
            </div>
            
            <!-- Moderators -->
            <div class="mb-12">
                <div class="flex items-center mb-6">
                    <div class="w-1 h-8 bg-green-500 mr-4 rounded-full"></div>
                    <h3 class="text-2xl font-bold text-white">Moderators</h3>
                </div>
                
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                    <% modMembers.forEach(member => { %>
                    <div class="staff-card bg-gray-800 rounded-lg p-6 flex items-center">
                        <div class="relative mr-4">
                            <img src="<%= member.avatar %>" 
                                 alt="<%= member.username %> Avatar" 
                                 class="w-16 h-16 rounded-full border-2 border-green-500">
                            <div class="absolute bottom-0 right-0 w-4 h-4 
                            <%= member.presence === 'online' ? 'bg-green-500' : member.presence === 'idle' ? 'bg-yellow-500' : member.presence === 'dnd' ? 'bg-red-500' : 'bg-gray-500' %> 
                            rounded-full border-2 border-gray-800"></div>
                        </div>
                        <div>
                            <div class="flex items-center">
                                <h4 class="text-lg font-semibold text-white"><%= member.globalName %></h4>
                                <span class="role-badge role-moderator ml-2">Mod</span>
                            </div>
                            <p class="text-gray-400 text-sm">@<%= member.username %></p>
                        </div>
                    </div>
                    <% }) %>
                </div>
            </div>
            
            <!-- Contributors -->
            <div>
                <div class="flex items-center mb-6">
                    <div class="w-1 h-8 bg-yellow-500 mr-4 rounded-full"></div>
                    <h3 class="text-2xl font-bold text-white">Contributors</h3>
                </div>
                
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                    <!-- Staff Card 10 
                    <div class="staff-card bg-gray-800 rounded-lg p-6 flex items-center">
                        <div class="relative mr-4">
                            <img src="https://cdn.discordapp.com/avatars/123456789012345678/a_abcdef1234567890abcdef1234567890.webp?size=128" 
                                 alt="Contributor Avatar" 
                                 class="w-16 h-16 rounded-full border-2 border-yellow-500">
                            <div class="absolute bottom-0 right-0 w-4 h-4 presence-online rounded-full border-2 border-gray-800"></div>
                        </div>
                        <div>
                            <div class="flex items-center">
                                <h4 class="text-lg font-semibold text-white">Tom</h4>
                                <span class="role-badge role-contributor ml-2">Contributor</span>
                            </div>
                            <p class="text-gray-400 text-sm">@tom</p>
                            <p class="text-gray-300 mt-1 text-sm">Builder & Texture Artist</p>
                        </div>
                    </div> -->
                    
                    <p class="text-gray-400 text-center col-span-full">
                        We don't have any contributors at the moment, you can donate to the server to get contributors and your name will be displayed here.
                    </p>
                </div>
            </div>
        </div>
    </div>


    <!-- Staff Section -->
    <div class="bg-gray-900 py-16">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
                <h2 class="text-3xl font-bold text-white">Statistics</h2>
            </div>
            <div class="grid grid-cols-2 md:grid-cols-3 gap-8 text-center">
                <div class="bg-gray-800 p-6 rounded-lg shadow-lg">
                    <div class="text-3xl font-bold text-blue-400 mb-2"><%= bot.guilds.cache.get("954173179042091028").memberCount %></div>
                    <div class="text-gray-300">Discord Member</div>
                </div>
                <div class="bg-gray-800 p-6 rounded-lg shadow-lg">
                    <div class="text-3xl font-bold text-blue-400 mb-2"><%= bot.guilds.cache.get("954173179042091028").roles.cache.get("1286332337499410462").members.size%></div>
                    <div class="text-gray-300">Total Staff</div>
                </div>
                <div class="bg-gray-800 p-6 rounded-lg shadow-lg">
                    <div class="text-3xl font-bold text-blue-400 mb-2"><%= bot.guilds.cache.get("954173179042091028").roles.cache.get("1377700384704827583").members.size%></div>
                    <div class="text-gray-300">Total Contributor</div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <%- include("./layouts/footer.ejs"); %>

    <script src="./static/js/navbarScript.js" type="text/javascript"></script>

    <script>
  function copyIP() {
        const ip = "play.acronet.work";
        navigator.clipboard.writeText(ip).then(() => {
        alert("IP berhasil disalin: " + ip);
    }).catch(err => {
        alert("Gagal menyalin IP.");
        console.error(err);
    });
    }
</script>

<script>
  async function updatePlayerCount() {
    try {
      const response = await fetch('https://api.mcsrvstat.us/3/play.acronet.work');
      const data = await response.json();

      const online = data.players?.online ?? 0;
      const max = data.players?.max ?? 0;

      document.getElementById("player-count").textContent = `${online} / ${max}`;
    } catch (error) {
      console.error("Gagal mengambil data player:", error);
      document.getElementById("player-count").textContent = "Status tidak tersedia";
    }
  }

  // Panggil saat halaman dibuka
  updatePlayerCount();

  // Perbarui setiap 30 detik
  setInterval(updatePlayerCount, 30000);
</script>

<script>
  async function fetchServerStatus() {
    try {
      const response = await fetch('https://api.mcsrvstat.us/3/play.acronet.work');
      const data = await response.json();

      const serverDot = document.getElementById('server-status-dot');
      if (data.online) {
        serverDot.classList.remove('bg-red-500');
        serverDot.classList.add('bg-green-500');
      } else {
        serverDot.classList.remove('bg-green-500');
        serverDot.classList.add('bg-red-500');
      }
    } catch (error) {
      const serverDot = document.getElementById('server-status-dot');
      serverDot.classList.remove('bg-green-500');
      serverDot.classList.add('bg-red-500');
    }
  }

  fetchServerStatus();
  setInterval(fetchServerStatus, 60000);
</script>

<script>
  fetch("https://discord.com/api/guilds/954173179042091028/widget.json")
    .then(response => response.json())
    .then(data => {
      const members = data.presence_count || data.members.length;
      document.getElementById("discord-members").textContent = `${members} Online`;
    })
    .catch(error => {
      document.getElementById("discord-members").textContent = "Unavailable";
      console.error("Error fetching Discord members:", error);
    });
</script>
</body>
</html>