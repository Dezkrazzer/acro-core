<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Floating Navbar</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
        }
        .navbar {
            position: fixed;
            top: 20px;
            left: 50%;
            transform: translateX(-50%);
            background: white;
            display: flex;
            gap: 20px;
            padding: 10px 20px;
            border-radius: 50px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .nav-item {
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
            cursor: pointer;
            transition: transform 0.2s;
        }
        .nav-item:hover {
            transform: scale(1.2);
            background: #f0f0f0;
        }
        .nav-item img {
            width: 24px;
            height: 24px;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <div class="nav-item" onclick="alert('Home clicked!')">
            <img src="home-icon.png" alt="Home">
        </div>
        <div class="nav-item" onclick="alert('Search clicked!')">
            <img src="search-icon.png" alt="Search">
        </div>
        <div class="nav-item" onclick="alert('Notifications clicked!')">
            <img src="bell-icon.png" alt="Notifications">
        </div>
        <div class="nav-item" onclick="alert('Profile clicked!')">
            <img src="user-icon.png" alt="Profile">
        </div>
    </div>
</body>
</html>
